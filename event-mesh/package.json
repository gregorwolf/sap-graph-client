{
  "name": "consumer",
  "description": "Simple NodeJS Application consuming messages",
  "version": "0.0.1",
  "engines": {
    "node": "^22"
  },
  "dependencies": {
    "@sap/xb-msg": "^0.9.12",
    "@sap/xb-msg-env": "^0.9.8",
    "@sap/xsenv": "^6.1.0",
    "dotenv": "^17.4.0"
  },
  "scripts": {
    "start": "node consumer.js"
  }
}
